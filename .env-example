# .env-example — copy this file to .env and fill in real values.
# Never commit .env to version control.

# Example: override the config path if running from a non-standard location
# CONFIG_PATH=config/setup.json

# Example: set a default random seed via environment (optional — config/setup.json takes precedence)
# SIGNAL_SEED=42
